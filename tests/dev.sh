# shellcheck shell=bash

# # For Development Use Only (Comment the Code After Use)

# # CFF Validation

# cffr::cff_validate(here::here("CITATION.cff"))

# # Git

# # Update `CITATION.cff`` and `codemeta.json`` before pushing new tag!
# # Release the new tag and, them, force the v1 tag to point to it:

# git tag
# git tag --force v1
# git push origin v1 --force
# git tag -d <tagname>
